# Public website's own dependencies only. In HUB_MODE=remote (the mode
# Render deploys use — see docs/website_deployment.md) this app never
# imports the ops-hub package directly, so ops-hub's own dependencies
# (stripe, pyjwt, etc.) don't need to be installed here at all.
Flask==3.1.3
requests==2.34.2
gunicorn==23.0.0
